import React from 'react';
import PropTypes from 'prop-types';


const CartPage = () => (
  <div>
    CartPage Component
  </div>
);

CartPage.propTypes = {};

CartPage.defaultProps = {};

export default CartPage;
